export async function expandWithQwen(shortPrompt: string): Promise<string> {
  // TODO: replace with a real call to your fine-tuned Qwen2-VL endpoint.
  return `High-contrast, brand-consistent promo: ${shortPrompt}. Use brand red (#D32F2F) and white, bold geometric sans typography, modern glossy gradient, centered headline, subtle drop shadow, clean composition, 16:9 banner for web hero.`;
}
